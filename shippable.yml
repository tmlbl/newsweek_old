language: node_js

node_js:
  - "0.10"
  - "0.11"

before_script:
  - useradd -m influxdb
  - wget http://s3.amazonaws.com/influxdb/influxdb_latest_amd64.deb
  - sudo dpkg -i influxdb_latest_amd64.deb

script:
  - grunt test

